"""Unified GA tuner package for the four evolvable AIs.

The package collects all four legacy ``evolve_*`` scripts into one
``python -m scripts.evolve`` entry point. See :mod:`scripts.evolve.__main__`
for the CLI and :mod:`scripts.evolve.profiles` for the per-AI registry.
"""
